/*import type { PartInfo } from "@/app/page";

interface InfoPanelProps {
  info: PartInfo;
}

export default function InfoPanel({ info }: InfoPanelProps) {
  return (
    <div className="p-20">
      <h2 className="text-xl font-bold mb-4">Selected Part</h2>
      <article className="max-w-3x1">
        <h1 className="text-2xl font-semibold text-gray-900 mb-4 mt-8">{info?.title || "No part selected"}</h1>
        <p className="text-lg text-gray-700 mb-6 leading-relaxed">{info?.body || "Select a part of the model to see information."}</p>
      </article>
    </div>
  )
}
  */